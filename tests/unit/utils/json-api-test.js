import jsonApi from 'twitch-client/utils/json-api';
import { module, test } from 'qunit';

module('Unit | Utility | json-api', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = jsonApi();
    assert.ok(result);
  });
});
